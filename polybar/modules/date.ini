[module/date]
type = internal/date
interval = 1

time = "%a, %b %d %H:%M:%S"
time-alt = "%a, %b %d %H:%M:%S"

format = <label>
format-padding = 1
format-prefix = %{T2}DATE%{T-}
format-prefix-foreground = ${colors.fg-alt}

label = %{T1}%time%%{T-}
label-foreground = ${colors.fg}
label-padding = 1
