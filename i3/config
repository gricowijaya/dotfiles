# This file has been auto-generated by i3-config-wizard(1).
# It will not be overwritten, so edit it as you like.
#
# Should you change your keyboard layout some time, delete
# this file and re-run i3-config-wizard(1).
#

# i3 config file (v4)
#
# Please see https://i3wm.org/docs/usergblacuide.html for a complete reference!

set $mod Mod4
# bindsym $mod+m exec --no-startup-id ~/.config/rofi/bin/menu_mpd
# bindsym $mod+space exec --no-startup-id ~/.config/rofi/bin/launcher_text
# bindsym $mod+p exec --no-startup-id ~/.config/rofi/bin/applet_backlight

# Font for window titles. Will also be used by the bar unless a different font
# is used in the bar {} block below.
#font pango: Terminess (TTF) Nerd Font Complete Mono 12
#font : Terminess (TTF) Bold Nerd Font Complete Mono 12

# polybar
exec --no-startup-id /home/gricowijaya/.config/polybar/launch.sh
# This font is widely installed, provides lots of unicode glyphs, right-to-left
# text rendering and scalability on retina/hidpi displays (thanks to pango).
# font pango:DejaVu Sans Mono 11

# The combination of xss-lock, nm-applet and pactl is a popular choice, so
# they are included here as an example. Modify as you see fit.

# xss-lock grabs a logind suspend inhibit lock and will use i3lock to lock the
# screen before suspend. Use loginctl lock-session to lock your screen.
exec --no-startup-id xss-lock --transfer-sleep-lock -- i3lock --nofork

# NetworkManager is the most popular way to manage wireless networks on Linux,
# and nm-applet is a desktop environment-independent system tray GUI for it.
exec --no-startup-id nm-applet

# Enable Floating Window
for_window [window_role="pop-up"] floating enable
for_window [window_role="task_dialog"] floating enable
for_window [class="ranger"] floating enable
for_window [class="rxvt-unicode"] floating enable

# Use pactl to adjust volume in PulseAudio.
set $refresh_i3status killall -SIGUSR1 i3status
bindsym XF86AudioRaiseVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ +10% && $refresh_i3status
bindsym XF86AudioLowerVolume exec --no-startup-id pactl set-sink-volume @DEFAULT_SINK@ -10% && $refresh_i3status
bindsym XF86AudioMute exec --no-startup-id pactl set-sink-mute @DEFAULT_SINK@ toggle && $refresh_i3status
bindsym XF86AudioMicMute exec --no-startup-id pactl set-source-mute @DEFAULT_SOURCE@ toggle && $refresh_i3status

# set wallpaper static
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Downloads/Chicago95/Plymouth/RetroTux/wallpaper.png
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Pictures/Wallpaper/Batman.jpg
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Pictures/Wallpaper/planet_omega-wallpaper-1400x1050.jpg
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Pictures/Wallpaper/wallpapersden.com_god-of-war-game-art_1400x1050.jpg
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Downloads/Wallpaper/red_cliff.jpg
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Programs/i3animated/scripts/nature/nature000.png
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Pictures/Wallpaper/i_am_root.jpg
# exec --no-startup-id feh --bg-scale /home/gricowijaya/Pictures/Wallpaper/mountain8bit.jpg

# set animated wallpaper
# exec --no-startup-id /home/gricowijaya/Programs/i3animated/scripts/loop_pngs.sh /home/gricowijaya/Programs/i3animated/scripts/fighting 0.1 

# exec picom automatically
exec --no-startup-id picom --experimental-backends 

# Theme
exec --no-startup-id wal -R

# Use Mouse+$mod to drag floating windows to their wanted position
floating_modifier $mod

# start a terminal
bindsym $mod+Return exec i3-sensible-terminal
bindsym $mod+Return+v split v exec i3-sensible-terminal
bindsym $mod+Return+c split h exec i3-sensible-terminal

# bindsym $mod+b exec brightnessctl 100%
# bindsym $mod+b exec brightnessctl 50%
# bindsym $mod+b exec brightnessctl 80%

# kill focused window
bindsym $mod+q kill

# FOR APPLICATION
# start dmenu (a program launcher)
# bindsym $mod+d exec --no-startup-id dmenu_run
# start rofi (another program launcher but kinda better i guess
# bindsym $mod+space exec rofi -show drun -theme sidebar -font "Perfect DOS VGA 437 12"

bindsym $mod+space exec rofi -show drun -theme sidebar -show=icons -font "Iosevka Term 11"
# bindsym $mod+space exec rofi -show drun -show=icons -font "JetBrainsMono Nerd Font Mono Bold 10"
# bindsym $mod+space exec rofi -show drun -theme sidebar -icon-theme "HighContrast" -show-icons -font "SFMono Nerd Font Mono Bold 10"

# There also is i3-dmenu-desktop which only displays applications shipping a
# .desktop file. It is a wrapper around dmenu, so you need that installed.
# bindcode $mod+40 exec --no-startup-id i3-dmenu-desktop
# To start Firefox just type $mod+o+f
set $mode_launcher Launch: [f]irefox

bindsym $mode+o mode "$mode_launcher"

mode "$mode_launcher" {
  bindsym f exec firefox
  #bindsym t exec thunderbird
  bindsym jk mode "default"
  bindsym Return mode "default"
}

# change focus
bindsym $mod+j focus down
bindsym $mod+k focus up
bindsym $mod+l focus right
bindsym $mod+h focus left

# alternatively, you can use the cursor keys:
bindsym $mod+Left focus left
bindsym $mod+Down focus down
bindsym $mod+Up focus up
bindsym $mod+Right focus right

# move focused window
bindsym $mod+Shift+j move down
bindsym $mod+Shift+k move up
bindsym $mod+Shift+l move right
bindsym $mod+Shift+h move left

# alternatively, you can use the cursor keys:
bindsym $mod+Shift+Left move left
bindsym $mod+Shift+Down move down
bindsym $mod+Shift+Up move up
bindsym $mod+Shift+Right move right

# split in horizontal orientation
bindsym $mod+c split h

# split in vertical orientation
bindsym $mod+v split v

# enter fullscreen mode for the focused container
bindsym $mod+f fullscreen toggle

# change container layout (stacked, tabbed, toggle split)
# bindsym $mod+s layout stacking
# bindsym $mod+w layout tabbed
bindsym $mod+e layout toggle split

# toggle tiling / floating
bindsym $mod+Shift+space floating toggle

# focus the parent container
# bindsym $mod+a focus parent

# focus the child container
#bindsym $mod+d focus child

# Define names for default workspaces for which we configure key bindings later on.
# We use variables to avoid repeating the names in multiple places.
set $ws1 "1"
set $ws2 "2"
set $ws3 "3"
set $ws4 "4"
set $ws5 "5"
set $ws6 "6"
set $ws7 "7"
set $ws8 "8"
set $ws9 "9"
set $ws10 "10"

assign [class="Terminal"] $ws1
assign [class="Firefox"] $ws2

# switch to workspace
bindsym $mod+1 workspace number $ws1
bindsym $mod+2 workspace number $ws2
bindsym $mod+3 workspace number $ws3
bindsym $mod+4 workspace number $ws4
bindsym $mod+5 workspace number $ws5
bindsym $mod+6 workspace number $ws6
bindsym $mod+7 workspace number $ws7
bindsym $mod+8 workspace number $ws8
bindsym $mod+9 workspace number $ws9
bindsym $mod+0 workspace number $ws10

# move focused container to workspace
bindsym $mod+Shift+1 move container to workspace number $ws1
bindsym $mod+Shift+2 move container to workspace number $ws2
bindsym $mod+Shift+3 move container to workspace number $ws3
bindsym $mod+Shift+4 move container to workspace number $ws4
bindsym $mod+Shift+5 move container to workspace number $ws5
bindsym $mod+Shift+6 move container to workspace number $ws6
bindsym $mod+Shift+7 move container to workspace number $ws7
bindsym $mod+Shift+8 move container to workspace number $ws8
bindsym $mod+Shift+9 move container to workspace number $ws9
bindsym $mod+Shift+0 move container to workspace number $ws10

# reload the configuration file
bindsym $mod+Shift+c reload
# restart i3 inplace (preserves your layout/session, can be used to upgrade i3)
bindsym $mod+Shift+r restart
# exit i3 (logs you out of your X session)
bindsym $mod+Shift+e exec "i3-nagbar -t warning -m 'You pressed the exit shortcut. Do you really want to exit i3? This will end your X session.' -B 'Yes, exit i3' 'i3-msg exit'"

# resize window (you can also use the mouse for that)
mode "resize" {
        # These bindings trigger as soon as you enter the resize mode

        # Pressing left will shrink the window’s width.
        # Pressing right will grow the window’s width.
        # Pressing up will shrink the window’s height.
        # Pressing down will grow the window’s height.
        bindsym h resize shrink width 10 px or 10 ppt
        bindsym j resize grow height 10 px or 10 ppt
        bindsym k resize shrink height 10 px or 10 ppt
        bindsym l resize grow width 10 px or 10 ppt

        # same bindings, but for the arrow keys
        bindsym Left resize shrink width 10 px or 10 ppt
        bindsym Down resize grow height 10 px or 10 ppt
        bindsym Up resize shrink height 10 px or 10 ppt
        bindsym Right resize grow width 10 px or 10 ppt

        # back to normal: Enter or Escape or $mod+r
        bindsym Return mode "default"
        bindsym Escape mode "default"
        bindsym $mod+r mode "default"
}

bindsym $mod+r mode "resize"


# Start i3bar to display a workspace bar (plus the system information i3status
# finds out, if available)
#bar {
#          mode hide
#          # hidden_state hide
#          #modifier Mod3
#          height 25 
#          position top
#          status_command LC_ALL=C i3status --config /home/gricowijaya/.config/i3/i3status.conf # basic configuration
#          separator_symbol "|"
#          tray_output primary
          
#          # $i3-theme-bar
#          colors {
#            # the background bar depends on what wallpaper on the background

#            separator #727176
#            # grey
#            # background #42414D
#            # inactive_workspace #41414D #41414D #DCDCDC
#            # black 
#            # background #000000
#            # green
#            background #0d0d11111717
#            inactive_workspace #0d0d11111717 #0d0d11111717 #B8BC88
#            # statusline #C2E1FF
#            statusline #B8BC88

#            focused_workspace #0d0d11111717 #0d0d11111717 #DCDCDC
#            active_workspace #0d0d11111717 #0d0d11111717 #B8BC88

#            # Daft Punk Theme use black for background
#            #focused_workspace #FFA652 #FFA652 #ffffff
#            #active_workspace #FFA652 #FFA652 #000000
            
#            #separator #FCEBDB
#            #focused_workspace #0088CC #0088CC #ffffff

#            #White on black
#            #background #FFFFFF
#            #statusline #000000
#            #focused_workspace #9D1C17 #9D1C17 #ffffff
#            #separator #FFFFFF
#            #active_workspace #9D1C17 #9D1C17 #000000
#            #inactive_workspace #000000 #000000 #9D1C17

#            #Red on  black
#            # background #000000
#            # statusline #FFFFFF
#            # focused_workspace #9D1C17 #9D1C17 #ffffff
#            # separator #000000
#            # active_workspace #9D1C17 #9D1C17 #000000
#            # inactive_workspace #000000 #000000 #9D1C17

#            #Black on Red
#            #background #9D1C17
#            #statusline #000000
#            #separator #9D1C17
#            #focused_workspace #000000 #000000 #ffffff
#            #active_workspace #000000 #000000 #9D1C17
#            #inactive_workspace #9D1C17 #9D1C17 #000000

#            #Blue
#            # focused_workspace #35F4A7 #35F4A7 #ffffff
#            # active_workspace #333333 #333333 #888888
#            # inactive_workspace #333333 #333333 #888888
#            # urgent_workspace   #2f343a #900000 #ffffff
#          }
#}

#bindsym $mod + s bar hidden_state show
#bindsym --release $mod + g bar hidden_state hide

# i3 gaps
# Run i3-gaps instead of i3 by default
# i3-gaps will have black bar on top if you have description borders

for_window [class=".*"] border pixel 0 
gaps inner 6 

smart_gaps on
smart_gaps inverse_outer

# Activate smart borders (always)
smart_borders on
smart_borders no_gaps

# Activate smart borders (only when ther are effectively no gaps)
smart_borders no_gaps

# Hide edge borders only if there is one window with no gaps
# hide_edge_borders smart_no_gaps

# gaphor
# bindsym $mod+g exec .local/bin/gaphor

# ranger(fileExplorer)
bindsym $mod+Shift+g exec i3-sensible-terminal -e ranger
# bpytop
bindsym $mod+Shift+b exec i3-sensible-terminal -e bpytop
# firefox
bindsym $mod+Shift+f exec firefox
for_window [class="firefox"] move to workspace $ws2
# discord
bindsym $mod+Shift+d exec discord
for_window [class="discord"] move to workspace $ws3
for_window [class="Spotify"] move to workspace $ws4
# razer polychromatic
for_window [class="polychromatic"] move to workspace $ws4
# steam
for_window [class="steam"] move to workspace $ws4
# spotify-tui
bindsym $mod+Shift+t exec i3-sensible-terminal -e spt
# pavucontrol (Volume Control)
bindsym $mod+Shift+p exec i3-sensible-terminal -e pavucontrol 
# flameshot
bindsym $mod+Shift+s exec i3-sensible-terminal -e flameshot gui >> /dev/null
# Print screen focused window
bindsym Print exec scrot -u '%Y%m%d_%H%M%S.png' -e 'mv $f ~/Pictures/Screenshots/'

# Print screen entire view
bindsym $mod+Print exec scrot '%Y%m%d_%H%M%S.png' -e 'mv $f ~/Pictures/Screenshots/'

mode "exit: [l]ogout, [r]eboot, [s]hutdown" {
  bindsym l exec i3-msg exit
  bindsym r exec systemctl reboot
  bindsym s exec systemctl shutdown
  bindsym Escape mode "default"
  bindsym Return mode "default"
}

bindsym $mod+x mode "exit: [l]ogout, [r]eboot, [s]hutdown"

### COLORS ###
# Set colors from Xresources
# Change 'color7' and 'color2' to whatever colors you want i3 to use
# from the generated scheme.
# NOTE: The '#f0f0f0' in the lines below is the color i3 will use if
# it fails to get colors from Xresources for some reason.
set_from_resource $fg i3wm.color2 #f0f0f0
set_from_resource $bg i3wm.color10 #f0f0f0

# class                 border  backgr. text indicator child_border
client.focused          $bg     $bg     $fg  $bg       $bg
client.focused_inactive $bg     $bg     $fg  $bg       $bg
client.unfocused        #000000 #000000 #000000 #000000 #000000
client.urgent           $bg     $bg     $fg  $bg       $bg
client.placeholder      $bg     $bg     $fg  $bg       $bg

client.background       $bg

# PROTIP: You can also dynamically set dmenu's colors this way:
bindsym $mod+d exec dmenu_run -nb "$fg" -nf "$bg" -sb "$bg" -sf "$fg"

# archlinux theme for i3 by okraits
# http://okraits.de

# media controls
bindsym $mod+i exec --no-startup-id "playerctl previous"
bindsym $mod+o exec --no-startup-id "playerctl play-pause"
bindsym $mod+p exec --no-startup-id "playerctl next"
